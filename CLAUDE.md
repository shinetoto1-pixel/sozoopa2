# 프로젝트 컨벤션

sozoopa(블로그 포스팅 작업)에서 갈라져 나온 프로젝트. 원래 sozoopa는 뉴스수집(1단계)·테마판단(2단계)·
포스팅작성(3단계)이 한 대화 세션 안에서 순서대로 진행됐는데, 이 프로젝트는 그 세 단계를 완전히 분리해서
각자 독립적으로(필요하면 무인 헤드리스로도) 돌아갈 수 있게 재구성한 것이다(2026-07-31 결정).

**sozoopa는 계속 그대로 운영 중이다** — 이 프로젝트는 검증 전까지 병행 운영하고, 문제없이 며칠 굴러가면
sozoopa를 은퇴시키고 이쪽으로 전환한다. 그 전까지는 실제 매일 포스팅은 sozoopa 쪽에서 나간다.

## 폴더 구조

```
1단계_수집/원칙.md   ← 뉴스 수집 로직 (시간창, 섹션 우선순위, URL수집→헤드라인 1차·2차 스크리닝 절차.
                     본문확보는 2단계 판단실행.ps1로 이관됨, 2026-08-03)
2단계_판단/
  원칙.md             ← 옥석가리기 판단 로직 (신선도 vs 빈도수, 단기촉매 필터, 종목매핑)
  테마목록.md / 테마패턴매핑.md / 테마종목트리.md  ← 이 프로젝트에서 새로 쌓는 판단 참고자료
    (테마종목트리.md는 antwinner.com에서 누적하는 테마↔종목 매핑 — 사후검증 방문 때 같이 채움)
  scripts/판단실행.ps1      ← 2단계 헤드리스(대화 세션 없이) 실행 스크립트
3단계_포스팅/
  원칙.md             ← 초안 작성 규칙 + 이미지 생성 절차 (문체·구성 순서는 아직 미완성 — 문서 안 "다음에 할 일" 참고)
  scripts/                  ← make_chart.py 등 차트·이미지 생성 스크립트
  assets/                   ← 메인이미지 PPT 템플릿 등
사후검증/                    ← 1·2·3단계와 무관한 별도 프로세스(원칙.md 참고). 3단계 포스팅 작성이 끝나면
                              자동으로 이어서 직전 영업일 몫을 검증(antwinner.com 실측 대조). 테마검증로그.md 보관
                              (판단수정패턴.md는 다른 축 — "우리 픽이 실측과 맞았나"가 아니라 "2단계
                              자동판단을 사용자가 왜/어떻게 고쳤나"를 쌓는 파일, 표본 쌓이면 검토)
daily/YYYYMMDD/             ← 그날그날 산출물(수집로그.md, 판단로그.html, 초안.html 등)
```

각 원칙.md 상단에 "sozoopa의 어느 문서에서 언제 기준으로 분리해왔는지"가 적혀있다. 원문(sozoopa)이
그 뒤로 갱신되면 이 문서들은 자동으로 따라가지 않으므로, 두 프로젝트가 병행되는 동안 로직이 갈라질 수 있다는
점을 감안한다.

## 파이프라인 진행 순서 (2026-08-10 확정)

매일 순서대로 진행하는 단계다. **각 단계가 끝나면 사용자가 다음 지시를 하지 않아도 먼저 "다음은
OO입니다. 진행할까요?"라고 물어본다** — 사용자가 매번 다음 단계를 일일이 말해야 하는 걸 피하기 위함
(2026-08-10 사용자 지시: "내가 얘기하지않더라도 네가 다음은 이거 해야합니다 진행할까요? 하고
물어봐주는게 좋겠다"). 사용자가 "쭉 진행해"처럼 여러 단계를 한 번에 승인하면 그 범위까지는 멈추지
않고 이어서 진행한다.

| 순서 | 단계 | 실행 | 끝나면 다음 |
|---|---|---|---|
| 1 | 1단계 URL수집 | `collect_urls.py --date YYYYMMDD`(먼저 news.daum.net에서 실제 시각 교차확인) | 헤드라인 1차 스크리닝 |
| 2 | 1단계 1차 스크리닝 | `헤드라인스크리닝실행.ps1 -Date` | 헤드라인 2차 스크리닝 |
| 3 | 1단계 2차 스크리닝 | `헤드라인2차스크리닝실행.ps1 -Date` | 2단계 클러스터링 |
| 4 | 2단계 클러스터링 | `키워드추출실행.ps1 -Date` | 2단계 판단실행 |
| 5 | 2단계 판단실행 | `판단실행.ps1 -Date`(보통 8분+ 걸려 백그라운드 실행) | 사용자 검토/재구성 → 3단계 |
| 6 | 3단계 초안 작성 | 차트(`make_chart.py`)·시장지표(`make_market_summary.py`)·메인이미지(`make_main_image.ps1`) 생성 → 초안.html·초안_구글블로그.html 작성 | 사후검증(직전 영업일) |
| 7 | 사후검증 | antwinner.com `/theme-trends` 대조 → `daily/N-1/사후검증.md` 기록(`사후검증/원칙.md` 절차) | 그날 파이프라인 종료. 세션 마무리 신호 있으면 세션로그 정리+커밋 |

## 세션 시작 시 필수 확인

**`git pull`은 사용자가 명시적으로 시키지 않는 한 먼저 실행하지 않는다**(2026-08-10 사용자 지시 —
"왜 자꾸 니맘대로 pull을 하는건데. 하랄때만 해". 다른 환경에서 작업했을 가능성이 있으면 pull이
필요한지 먼저 물어보고, 확인받은 뒤에만 실행). 새 세션에서는 대신 `세션로그.md`의 가장 최근 1~2개
날짜 항목만 확인한다. 의미있는 결정(구조 변경, 새 컨벤션 등)이 있었던 세션이 끝나면 `세션로그.md`에
새 항목을 날짜별로 추가한다.

## 세션 마무리 신호

sozoopa의 CLAUDE.md와 동일한 규칙을 따른다 — "퇴근해야겠다", "오늘은 여기까지" 등 다른 환경에서 이어가겠다는
취지의 말이면 표현이 달라도 전부 같은 신호로 인식하고, 자동으로 세션로그 정리 + 커밋 + 푸시 후 짧게 확인해준다.

## 단축 명령어

- **"깃 그거해줘"** = 지금까지의 변경사항을 git에 커밋하고 원격 저장소에 푸시해줘
  (커밋 메시지는 변경 내용을 보고 알아서 작성, 별도 확인 없이 진행)

## 코드 작업 시 행동 지침 (scripts/ 등 코드 수정·추가할 때 적용)

속도보다 신중함 쪽에 무게를 둔 지침. 사소한 작업엔 판단해서 유연하게 적용.

0. **수정하기 전에 먼저 물어보고 확인받는다**(2026-08-13 — 차트 왼쪽 여백 문제를 사용자 확인 없이 바로
   고쳐서 지적받음: "이렇게 수정되네요 반영할까요? 하고 물어보고 수정을 해야지 왜 니멋대로 하는거야?").
   원인·해결 방향을 찾아도 바로 파일을 고치지 말고, 먼저 "이렇게 고치면 될 것 같은데 반영할까요?"라고
   물어본 뒤 확인받고 수정한다. 테스트 목적으로 별도 파일에 결과물을 만들어 보여주는 건 괜찮지만(예:
   `_test_*.png`), 원본 스크립트(`scripts/` 안 `.py`/`.ps1` 등) 자체를 고치는 건 반드시 확인 후에 한다.
1. **코딩 전에 먼저 생각하기** — 추측 금지, 헷갈리면 숨기지 말고 드러낼 것. 가정은 명시적으로 말하기.
   해석이 여러 개면 옵션을 제시하기(혼자 골라 진행 금지). 더 단순한 방법이 있으면 말하고 필요하면 반박도 하기.
   불명확하면 멈추고 뭐가 헷갈리는지 짚어서 질문하기.
2. **단순함 우선** — 문제 해결에 필요한 최소한의 코드만. 요청받지 않은 기능·추상화·유연성 추가 금지.
   일어나지 않을 상황에 대한 에러 핸들링 금지.
3. **외과수술식 변경** — 건드려야 할 것만 건드리기. 기존 코드 수정 시 인접 코드·주석·포맷팅 "개선" 금지,
   안 망가진 걸 리팩터링 금지, 기존 스타일 유지. 내 변경으로 안 쓰게 된 것만 정리하고, 원래 있던 죽은
   코드는 언급만 하고 삭제하지 않기.
4. **목표 지향 실행** — 작업을 검증 가능한 목표로 변환("버그 고쳐줘" → "재현 테스트 작성 후 통과시키기").
   여러 단계면 "단계 → 검증방법" 형태로 짧게 계획 먼저 밝히기.

## 2단계 독립 실행

대화 세션 없이 터미널에서 바로 판단만 돌리고 싶을 때, 순서대로 두 스크립트를 실행한다(둘 다
`daily/<날짜>/2차헤드라인스크리닝.md`가 있어야 함 — 1단계 헤드라인 2차 스크리닝까지 끝난 상태):

```powershell
.\2단계_판단\scripts\키워드추출실행.ps1 -Date 20260803   # 클러스터후보.md 생성(배치 병렬, 수초~수십초)
.\2단계_판단\scripts\판단실행.ps1 -Date 20260803          # 클러스터후보.md로 3축평가~본문확보, 판단로그.md·
                                                          # 포스팅작성뉴스및종목.md 생성
```

Claude 데스크톱 앱이 설치된 컴퓨터라면 이 대화창이 아니어도, 다른 세션·다른 터미널에서도 동일하게 동작한다.
